const {reverse}= require("dns");
const reverseString = require('./stringUtils');

test("", () => {// description -what we are teting
    expect(reverseString("hannah")).toBe("hannah");//actual logic of te test
});